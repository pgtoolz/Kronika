# Класс 1: операционная система и cgroup

[English version](os.md)

Этот справочник связывает записанные показатели Linux с файлами-источниками и пределами чтения. Данные одного типа хранятся в секции сегмента. Идентификаторы типов ОС занимают `1_100_001`–`1_299_999`; точные поля определены в [реестре](../../crates/kronika-registry/src/codec). Формулы показателей интерфейса вынесены в [справочник Linux](../metrics-linux.ru.md). Cgroup — группа процессов Linux с общим учётом и ограничениями CPU, памяти и других ресурсов.

Поле `scope`, если оно есть в строке, указывает, к какой машине, pod или контейнеру относятся данные:

| Код | Область |
| ---: | --- |
| `0` | узел (`host`) |
| `1` | pod — группа контейнеров |
| `2` | сетевое пространство имён pod (`pod_net`) |
| `3` | контейнер |
| `4` | не определено |

CPU, память, диски, точки монтирования и топология описывают машину, даже если сборщик находится в контейнере. При записанном контейнерном окружении сетевые секции используют `pod_net`. Процессы получают `container` внутри контейнера и `host` вне него. Обнаружение cgroup v2 работает на машинах и в контейнерах; строки выбранного предка используются для графиков ресурсов контейнера.

Корни файловых систем можно переопределить через `KRONIKA_PROC_ROOT` (по
умолчанию `/proc`) и `KRONIKA_SYS_ROOT` (по умолчанию `/sys`).

## Зарегистрированные типы

`snapshot_full` означает полный снимок строк секции в момент сбора; `on_change` — запись при изменении. Ключ сортировки задаёт порядок строк внутри секции, а `ts` — время записи в микросекундах Unix.

| `type_id` | Источник | Семантика | Ключ сортировки |
|-----------|----------|-----------|-----------------|
| `1_100_001` | `/proc/PID/{stat,status,io,cmdline}`, часто собираемый набор | `snapshot_full` | `(pid, ts)` |
| `1_101_001` | `/proc/PID/status`, расширенный набор | `snapshot_full` | `(pid, ts)` |
| `1_102_001` | строки CPU из `/proc/stat` | `snapshot_full` | `(cpu_id, ts)` |
| `1_103_001` | одиночные показатели из `/proc/stat` и `/proc/uptime` | `snapshot_full` | `(ts)` |
| `1_104_001` | `/proc/meminfo` | `snapshot_full` | `(ts)` |
| `1_105_001` | `/proc/loadavg` | `snapshot_full` | `(ts)` |
| `1_106_001` | `/proc/vmstat` | `snapshot_full` | `(ts)` |
| `1_107_001` | `/proc/pressure/*`, файлы ожидания ресурсов cgroup | `snapshot_full` | `(resource, ts)` |
| `1_108_001` | `/proc/diskstats` | `snapshot_full` | `(major, minor, ts)` |
| `1_109_001` | `/proc/net/dev` и сведения о соединении из sysfs | `snapshot_full` | `(iface, ts)` |
| `1_110_001` | `/proc/net/snmp` | `snapshot_full` | `(ts)` |
| `1_111_001` | `/proc/net/netstat` | `snapshot_full` | `(ts)` |
| `1_112_002` | `mountinfo` и ограниченный вызов `statvfs` для байтов и inode локальных файловых систем | `on_change` | `(major, minor, mount_point, ts)` |
| `1_113_001` | `/proc/cpuinfo` и топология из sysfs | `on_change` | `(cpu_id, ts)` |
| `1_114_001` | `/proc/interrupts` | `snapshot_full` | `(irq, ts)` |
| `1_115_001` | `/proc/softirqs` | `snapshot_full` | `(vector, ts)` |
| `1_116_001` | `/proc/sys/fs/{file-nr,inode-nr,dentry-state}` | `snapshot_full` | `(ts)` |
| `1_117_001` | `/sys/devices/system/node/node*/meminfo` | `snapshot_full` | `(node_id, ts)` |
| `1_118_001` | `/proc/net/snmp6` | `snapshot_full` | `(ts)` |
| `1_119_001` | `/proc/net/rpc/nfs` | `snapshot_full` | `(ts)` |
| `1_120_001` | `/proc/net/rpc/nfsd` | `snapshot_full` | `(ts)` |
| `1_121_001` | состав политики CPUFreq, драйвер, источник и аппаратный диапазон из sysfs | `on_change` | `(policy_id, ts)` |
| `1_122_001` | частоты политики CPUFreq, разрешённый диапазон и число активных CPU из sysfs | `snapshot_full` | `(policy_id, ts)` |
| `1_123_001` | точные связи блочных устройств из sysfs: раздел с целым устройством, слой dm/LVM/MD с каждым нижним устройством | `on_change` | `(major, minor, parent_major, parent_minor, ts)` |
| `1_124_002` | соответствия UID наблюдаемых процессов именам из `/etc/passwd` | `on_change` | `(scope, uid, ts)` |
| `1_200_001` | cgroup: сопоставление процессов | `snapshot_full` | `(pid, ts)` |
| `1_201_001` | cgroup: cpu | `snapshot_full` | `(cgroup_path, ts)` |
| `1_201_002` | cgroup: cpu с эффективным cpuset, сохранённый формат чтения | `snapshot_full` | `(cgroup_path, ts)` |
| `1_201_003` | выбранная cgroup: CPU с идентификатором каталога | `snapshot_full` | `(cgroup_path, cgroup_identity, ts)` |
| `1_202_001` | cgroup: memory | `snapshot_full` | `(cgroup_path, ts)` |
| `1_202_002` | cgroup: memory с разделяемой памятью, сохранённый формат чтения | `snapshot_full` | `(cgroup_path, ts)` |
| `1_202_003` | выбранная cgroup: память с идентификатором каталога | `snapshot_full` | `(cgroup_path, cgroup_identity, ts)` |
| `1_203_002` | cgroup: io с независимо необязательными счётчиками устройства | `snapshot_full` | `(cgroup_path, major, minor, ts)` |
| `1_203_003` | выбранная cgroup: ввод-вывод с идентификатором каталога | `snapshot_full` | `(cgroup_path, cgroup_identity, major, minor, ts)` |
| `1_204_001` | cgroup: pids | `snapshot_full` | `(cgroup_path, ts)` |
| `1_205_001` | прежний контекст cgroup сборщика | `snapshot_full` | `(ts)` |
| `1_205_002` | контекст выбранного предка, идентификаторы и видимые корни монтирования | `snapshot_full` | `(ts)` |
| `1_206_001` | видимые каталоги cgroup v2, корни монтирования и идентификаторы родителей | `snapshot_full` | `(cgroup_path, cgroup_identity, ts)` |
| `1_207_001` | учёт CPU и лимиты обнаруженных cgroup v2 | `snapshot_full` | `(cgroup_path, cgroup_identity, ts)` |
| `1_208_001` | память, лимиты, локальные и иерархические события обнаруженных cgroup v2 | `snapshot_full` | `(cgroup_path, cgroup_identity, ts)` |
| `1_209_001` | потоки, лимиты и источник событий обнаруженных cgroup v2 | `snapshot_full` | `(cgroup_path, cgroup_identity, events_source, ts)` |
| `1_210_001` | I/O по устройствам обнаруженных cgroup v2 | `snapshot_full` | `(cgroup_path, cgroup_identity, major, minor, ts)` |

Сейчас для сбора требуется cgroup v2. Без видимой иерархии v2 метрики cgroup и
связи процессов с группами недоступны; остальные включённые локальные источники
продолжают работать. Прежние записи cgroup v1 остаются читаемыми.

Основные строки ресурсов описывают самого верхнего видимого и читаемого предка
собственной группы сборщика. Для них используются `1_201_003`, `1_202_003` и
`1_203_003`; `1_205_002` хранит выбранные пути, видимые корни монтирования и
идентификаторы каталогов. Все контроллеры относятся к одной выбранной группе.
`scope=4` не приписывает ей принадлежность машине или pod. Отсутствующий файл
метрик не меняет выбор и не заменяется счётчиком дочерней группы. Изменение
`cgroup_identity` разрывает расчёт приростов. Квота и период CPU записываются
в микросекундах; их отношение даёт число ядер. Лимиты описывают выбранную группу
с учётом доступных ограничений предков. Отсутствующие лимиты неизвестны; для
памяти `max_unlimited` отличает записанное отсутствие ограничения от отсутствия
данных. Подробнее — в [справочнике Linux](../metrics-linux.ru.md#container-cgroups).

Все видимые и доступные каталоги cgroup v2 записываются в `1_206_001`
(`os_cgroup_v2_group`), а CPU, память, потоки и I/O по устройствам — в
`1_207_001`–`1_210_001`. Сбор охватывает пустые и промежуточные группы и видимые
монтирования cgroup2 вне `/sys/fs/cgroup`, на машинах и в контейнерах.
Он выполняется при запуске и по умолчанию каждые 30 секунд. Отсутствующие или
некорректные значения остаются null; отсутствие лимита записывается отдельно.
Счётчики групп и устройств могут включать потомков или нижележащие слои;
их нельзя учитывать дважды. См. [поля и область учёта событий](../metrics-linux.ru.md#container-cgroups).

Встроенные графики контейнера используют описанные выше строки выбранного
предка, в том числе `1_204_001` для числа потоков. Прежние форматы групп процессов
остаются читаемыми. Каждый счётчик I/O устройства может быть доступен независимо
от остальных. Принадлежность процессов к cgroup также собирается по умолчанию
каждые 30 секунд.

**Прежний контекст `1_205_001`.** Ниже описаны старые записи, включая cgroup v1,
а не текущий выбор предка. Форматы CPU и памяти `002` также остаются в реестре
для существующих файлов WAL и ZMS.

`os_cgroup_context` хранит точные пути контроллеров для процесса сборщика из
`/proc/self/cgroup`. В cgroup v2 для CPU, памяти и ввода-вывода записывается
единый путь. В cgroup v1 пути контроллеров остаются раздельными. Поле
`cpuset_cpus` содержит число CPU из `cpuset.cpus.effective` в v2 или
`cpuset.effective_cpus` в v1. Если нужный файл отсутствует или не разбирается,
поле равно `null`. При сборе cpuset сборщик не ищет значение у предка и не
подставляет число CPU хоста.

Поля `effective_cpu_quota_usec` и `effective_cpu_period_usec` хранят точную
пару с минимальным отношением квоты к периоду на пути от настроенного корня
cgroup до конечной cgroup процесса. Квота `-1` с положительным периодом
означает, что все обязательные уровни с файлами контроллера проверены и не
задают предела. Оба поля равны `null`, если обязательный уровень отсутствует
или содержит непригодное значение. В cgroup v2 файл `cpu.max` читается от корня
монтирования до конечной cgroup. Для принадлежности ниже корня только ошибка `NotFound` в
самом корне означает неограниченный настоящий корень; все дочерние файлы
обязательны. Другие ошибки корня не игнорируются, а принадлежность самому корню
требует читаемого корневого файла. В cgroup v1 сборщик однозначно привязывает
корень контроллера CPU по конечной cgroup, а затем читает `cpu.cfs_quota_us` и
`cpu.cfs_period_us` у каждого предка только из этого корня. Для квот `Q₁`, `Q₂` и периодов `P₁`, `P₂` сравнение выполняется без деления: `Q₁ × P₂ < Q₂ × P₁`. Все четыре величины измеряются в микросекундах.

Поле `effective_memory_max` хранит минимальное конечное значение `memory.max`
в той же точной иерархии cgroup v2 по тому же правилу корня монтирования. В
cgroup v1 используется проверенный `hierarchical_memory_limit` точной конечной
cgroup, который уже учитывает семантику иерархии ядра. Это значение и
`memory.limit_in_bytes` конечной cgroup должны читаться из одного привязанного
корня контроллера. Конечный иерархический лимит не может превышать конечный
локальный лимит, а неограниченный иерархический лимит не может сочетаться с
конечным локальным. Поле равно `null` для проверенного неограниченного значения
и когда эти данные не удалось прочитать согласованно. Значение `-1` и значения
v1, не меньшие половины `i64::MAX`, являются признаками отсутствия лимита и не
выдаются за байты.

`cgroup_version` равен `0`, если сведения о принадлежности не удалось прочитать
или сопоставить с деревом cgroup, из которого собираются данные. В cgroup v1
поле `cpu_path` равно `null`, когда контроллеры `cpu` и `cpuacct` помещают
процесс в разные пути: в этом случае нет одной строки CPU cgroup, где
одновременно есть потребление и квота. Путь контроллера также равен `null`,
если в текущем сохранённом формате нет хотя бы одного счётчика или компонента,
который показывает веб-интерфейс: неполные старые файлы не представляются нулями.
Локальные столбцы квоты CPU и лимита памяти по-прежнему показывают точные файлы
конечной cgroup и не переименовываются в эффективные значения иерархии.

Период сбора не входит в `type_id`. Планировщик сборщика задаёт его для
каждого источника; интервалы и значения по умолчанию перечислены в
[README сборщика](../../bins/kronika-collector/README.ru.md).

CPUFreq объединяет CPU, частотой которых ядро управляет совместно; такая группа называется политикой. `1_121_001` хранит список `related_cpus`, драйвер, выбранный источник фактической частоты и аппаратные пределы в целых герцах. `1_122_001` хранит одно измерение на политику. При каждом чтении сборщик предпочитает успешно разобранный `cpuinfo_avg_freq`, затем `cpuinfo_cur_freq`; если оба недоступны, значение и источник равны `null`. `scaling_cur_freq` хранит отдельно заявленную или запрошенную рабочую частоту политики.

## Ограничения

Одна операция чтения procfs ограничена 4 MiB константой формата. Число строк в
снимке процессов не ограничено: хост с тридцатью тысячами процессов создаёт
тридцать тысяч строк. Для справочника пользователей действуют отдельные
ограничения, описанные ниже. Сообщение `segment_write_finish` в логе содержит
пиковый RSS в поле `rss_kib`.

## Единицы

Каждый столбец накопительного счётчика (`counter`) или обычного значения (`gauge`) объявляет единицу измерения. Проверка реестра отклоняет столбец без единицы. Используются следующие обозначения:

| Единица | Где используется |
| --- | --- |
| `none` | безразмерное число, например средняя нагрузка |
| `count` | объекты: процессы, потоки, дескрипторы, прерывания, пакеты |
| `bytes` | счётчики байтов |
| `kib` | значения в кибибайтах, которые ядро выводит в `/proc/meminfo` |
| `pages` | страницы памяти размером `instance_metadata.page_size_bytes` каждая |
| `sectors` | 512-байтные секторы из `/proc/diskstats` |
| `seconds`, `milliseconds`, `microseconds`, `nanoseconds` | время |
| `jiffies` | учётные такты CPU: `instance_metadata.clock_ticks_per_sec` в секунду |
| `hertz` | тактовая частота |
| `megabits_per_second` | согласованная скорость соединения |
| `percent`, `celsius` | объявлены и не используются в секциях ОС |

Описание полей `TypeContract` входит в программы чтения и записи при компиляции. В секциях сегмента хранятся значения без повторения единиц измерения.

## Семейства метрик

Таблицы ниже сопоставляют группы метрик префиксам идентификаторов секций. Формулы и способы получения общих итогов описаны в [справочнике Linux](../metrics-linux.ru.md).

### CPU

| Метрика | Секция |
| --- | --- |
| Время user/nice/system/idle/iowait/irq/softirq/steal/guest по каждому CPU и суммарно | `1_102` |
| Переключения контекста, создание процессов, очереди выполняемых и заблокированных задач | `1_103` |
| Общее число аппаратных прерываний и softirq | `1_103` |
| Время работы и суммарное время простоя | `1_103` |
| Средняя нагрузка и число процессов | `1_105` |
| Число прерываний по IRQ | `1_114` |
| Число прерываний по векторам softirq | `1_115` |
| Модель, ядро, сокет, максимальная частота | `1_113` |
| NUMA-узел для каждого CPU | `1_113` |
| Состав групп CPUFreq и история фактической и заявленной частоты | `1_121`, `1_122` |
| Инструкции и циклы (`perf`) | — (не собираются) |

### Память и swap

| Метрика | Секция |
| --- | --- |
| Общая, свободная и доступная память; буферы, кэш и структуры ядра (slab) | `1_104` |
| Изменённые страницы и их запись на диск, анонимная и отображённая память, общая память (shmem), таблицы страниц | `1_104` |
| Лимит резервирования и зарезервированная память | `1_104` |
| Огромные страницы: всего, свободно, размер | `1_104` |
| Огромные страницы: зарезервированные, избыточные, анонимные, общая память (shmem) | `1_104` |
| Подкачка (swap): всего, свободно, кэшировано | `1_104` |
| Стек ядра, память per-CPU, bounce, vmalloc, unevictable, mlocked | `1_104` |
| Пул zswap и размер сохранённых данных | `1_104` |
| Ввод и вывод страниц, ввод и вывод swap, ошибки страниц | `1_106` |
| Сканирование и освобождение страниц через kswapd и напрямую | `1_106` |
| Сканирование и освобождение страниц через khugepaged | `1_106` |
| Случаи остановки при выделении и уплотнении памяти | `1_106` |
| Миграция NUMA, успешная и неудачная миграция страниц | `1_106` |
| Выделение прозрачных огромных страниц | `1_106` |
| Повторные обращения и восстановление рабочего набора, освобождение памяти узла | `1_106` |
| Упреждающее чтение swap и попадания | `1_106` |
| Завершения процессов по OOM | `1_106` |
| Память каждого NUMA-узла | `1_117` |
| Общая память KSM, ZFS ARC, balloon | — (не собирается) |

### Задержки из-за нехватки ресурсов

| Метрика | Секция |
| --- | --- |
| Ожидание CPU, памяти и ввода-вывода (PSI): хотя бы одной задачи (`some`) или всех непростаивающих задач (`full`); средние за 10/60/300 секунд и накопленное время | `1_107` |

### Хранилище

| Метрика | Секция |
| --- | --- |
| Чтения, записи, секторы, слияния, время обслуживания и очереди | `1_108` |
| Освобождение блоков (discard) и сброс на накопитель (flush) | `1_108` |
| Активные запросы | `1_108` |
| Устройства LVM и MD | `1_108` (`/proc/diskstats` содержит их) |
| Loop- и RAM-устройства | Исключены старшие номера устройств `7` и `1` |
| Точки монтирования, тип и источник файловой системы | `1_112` |
| Общий и доступный объём файловой системы | `1_112` |
| Корень файловой системы и общее/доступное число inode | `1_112` |
| Точные связи раздела с родительским устройством | `1_123` |
| Файловые дескрипторы, индексные дескрипторы (inode), записи каталогов (dentry) | `1_116` |

Ёмкость файловой системы заполняется только для явно заданного списка локальных
типов: `ext2`, `ext3`, `ext4`, `xfs`, `btrfs`, `f2fs`, `zfs`, `tmpfs` и
`overlay`. Для сетевых, FUSE/userspace, `autofs` и неизвестных типов поля
ёмкости содержат `null`. Для всего прохода действует общий тайм-аут в одну
секунду; результаты, полученные до его истечения, сохраняются.

Поля идентификации `1_112_002` — `(major, minor, mount_point)`, поэтому две точки монтирования одной файловой
системы остаются разными. `root` — поле 4 из mountinfo. Записанные
`free_bytes = f_bavail × f_frsize`, `available_inodes = f_favail` берутся из `statvfs`. `1_123_001`
записывает по строке на каждую точную связь из sysfs: признак раздела с его
непосредственным родительским `dev` и слой dm/LVM/MD с каждым устройством из
`slaves/`. Для обычных целых устройств, неразрешённых ссылок sysfs и
происхождения bind mount связь не выводится. В контейнере `1_108_001`
сохраняет устройства с неслужебной точкой монтирования и устройства, которым
`io.stat` выбранной cgroup начисляет I/O, поэтому физические слои
под смонтированным томом остаются названными, а `1_123_001` хранит только связи
на цепочках под этими устройствами. `1_112_002` никогда не записывает точки
монтирования внутри `/proc` и `/sys`: среды выполнения контейнеров маскируют
там пути пустым tmpfs.

### Сеть

| Метрика | Секция |
| --- | --- |
| Байты, пакеты, ошибки, потери, переполнения FIFO, ошибки кадров и несущей, коллизии по каждому интерфейсу | `1_109` |
| Скорость соединения и дуплекс | `1_109` |
| Открытия, сбросы, сегменты, повторные передачи и установленные соединения TCP | `1_110` |
| Датаграммы и ошибки UDP, пакеты без порта назначения | `1_110` |
| Приём, доставка, пересылка, отбрасывание, пересборка и фрагментация IPv4 | `1_110` |
| Входящие и исходящие ICMP-пакеты, включая ошибки | `1_110` |
| Переполнения и потери очереди прослушивания, тайм-ауты и подробные данные о повторных передачах | `1_111` |
| Прерывания соединений, нехватка памяти, потери в очереди, очистка очереди приёма и отложенные ACK | `1_111` |
| Октеты полезной нагрузки на входе и выходе | `1_111` |
| IPv6, ICMPv6 и UDPv6 | `1_118` |
| RPC и операции клиента NFS | `1_119` |
| RPC, кэш ответов и ввод-вывод сервера NFS | `1_120` |
| Сетевой ввод-вывод по каждому процессу | — (не собирается) |

### Процессы

| Метрика | Секция |
| --- | --- |
| Идентификаторы: pid, ppid, uid, gid, имя, командная строка, время запуска | `1_100` |
| Имена реального и эффективного пользователя с сохранением числового UID | `1_100`, `1_124` |
| Состояние, потоки, приоритет, nice, политика, приоритет реального времени, текущий CPU | `1_100` |
| Пользовательское и системное время CPU | `1_100` |
| Задержка в очереди выполнения и при блочном вводе-выводе | `1_100` |
| Добровольные и принудительные переключения контекста | `1_100`, `1_101` |
| Малые и большие страничные промахи | `1_100` |
| Виртуальная и резидентная память, память в подкачке | `1_100` |
| Системные вызовы чтения и записи, символы и байты хранилища | `1_100` |
| Объём сегмента данных, стека, библиотек, заблокированной памяти и таблиц страниц; пиковый объём виртуальной и резидентной памяти | `1_101` |
| Размер таблицы файловых дескрипторов | `1_101` |
| cgroup процесса | `1_200` |
| Строки по каждому потоку, `wchan`, пропорциональная память (резидентная память с делением общих страниц между процессами) | — (не собираются) |

`os_user` записывает в открытый сегмент не более одного соответствия для
каждой наблюдаемой пары `(scope, uid)`. Кандидатами становятся только реальные
и эффективные UID успешно разобранных строк `os_process`. Новый UID,
появившийся позднее в том же сегменте, добавляется через обычный WAL; после
ошибки добавления он остаётся кандидатом для следующего окна. Строка хранит
время сбора, UID, ссылку на имя в словаре и принадлежность ресурсов `scope`.

Сборщик читает только `/etc/passwd` и только один раз для каждого открытого
сегмента. Размер чтения ограничен 256 KiB, строка — 4 KiB, разобранный снимок —
4 096 записями, имя пользователя — 256 байтами. Ошибочные и слишком длинные
записи пропускаются; слишком большой файл или число записей отключает только
запись имён в этом сегменте. Запросов к NSS, LDAP, SSSD или веб-сервисам
нет. Поэтому отсутствующие и динамические пользователи остаются числовыми в
API и интерфейсе.

Код чтения соединяет `os_process` только с `os_user` и `dict.strings` того же
сегмента по ключу `(scope, uid)`. Для условий поиска `user` и `effective_user` сервер сначала находит имена,
затем сортирует строки и разбивает их на страницы. Условия `user_id` и
`effective_user_id` сравнивают числовые UID. Обычный
текстовый поиск охватывает команду и оба найденных имени. При запросе данных
справочник пользователей машины, где открыт просмотр, не читается.

Таблица процессов показывает накопительные поля как скорости между снимками. Для полного процессорного времени с запуска есть отдельное поле `cpu_time_ticks = utime + stime`: сумма пользовательских и системных тактов CPU.

### cgroup и контейнер

| Метрика | Секция |
| --- | --- |
| Использование CPU: user, system, throttling, quota, period | `1_201` |
| Память: current, max, anon, file, kernel, slab, events, OOM | `1_202` |
| Байты и операции блочного ввода-вывода по каждому устройству | `1_203` |
| Потоки (TID): pids.current и pids.max | `1_204` |
| Форматы cgroup v1 и v2 | `1_200`–`1_205` |

## Что не собирается

| Несобираемая метрика | Интерфейс ядра или библиотеки |
| --- | --- |
| Инструкции, циклы и другие счётчики PMU | `perf_event_open` |
| Загрузка и память GPU | NVML через вспомогательный демон |
| Сетевой ввод-вывод по каждому процессу | Модуль ядра `netatop` |
| Счётчики портов Infiniband | `/sys/class/infiniband` |
| Занятость кэша последнего уровня и пропускная способность памяти | `/sys/fs/resctrl` |
| Строки по каждому потоку и `wchan` | `/proc/PID/task/*` |
| Пропорциональная память процесса | `/proc/PID/smaps_rollup` |
| KSM, ZFS ARC, баллонинг памяти гипервизором | `/sys/kernel/mm/ksm`, состояние модуля ZFS |
